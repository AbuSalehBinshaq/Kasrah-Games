# دليل الاختبار الشامل - نظام Kasrah Games\n\n## خطة الاختبار\n\n### 1. اختبار SDK JavaScript\n\n#### 1.1 اختبار التهيئة\n```javascript\n// ✓ يجب أن ينجح\nconst sdk = new KasrahSDK({\n  gameId: 'test-game-123',\n  apiKey: 'test-key-abc',\n  debug: true\n});\n\nconst initialized = await sdk.init();\nconsole.assert(initialized === true, 'Failed to initialize SDK');\n```\n\n#### 1.2 اختبار الإعلانات\n```javascript\n// اختبار إعلان الفيديو\nconst videoResult = await sdk.showVideoAd();\nconsole.assert(videoResult === true, 'Failed to show video ad');\n\n// اختبار إعلان المكافأة\nconst rewardResult = await sdk.showRewardedAd();\nconsole.assert(rewardResult === true, 'Failed to show rewarded ad');\n\n// اختبار الاستماع للأحداث\nsdk.on('rewardEarned', (data) => {\n  console.assert(data.reward > 0, 'Invalid reward amount');\n});\n```\n\n#### 1.3 اختبار البيانات السحابية\n```javascript\n// اختبار الحفظ\nconst saveResult = await sdk.saveData('testKey', { value: 'test' });\nconsole.assert(saveResult === true, 'Failed to save data');\n\n// اختبار التحميل\nconst loadedData = await sdk.loadData('testKey');\nconsole.assert(loadedData !== null, 'Failed to load data');\nconsole.assert(loadedData.value === 'test', 'Data mismatch');\n```\n\n#### 1.4 اختبار التحليلات\n```javascript\n// اختبار تتبع الأحداث\nconst trackResult = await sdk.trackEvent('testEvent', { test: true });\nconsole.assert(trackResult === true, 'Failed to track event');\n\n// اختبار إحصائيات الجلسة\nconst stats = sdk.getSessionStats();\nconsole.assert(stats.sessionId !== null, 'Missing session ID');\nconsole.assert(stats.adCount >= 0, 'Invalid ad count');\n```\n\n### 2. اختبار API Endpoints\n\n#### 2.1 اختبار Game ID API\n```bash\n# الحصول على معلومات اللعبة\ncurl -X GET \"https://kasrah-games.onrender.com/api/sdk/games/manage?gameId=test-game-123\"\n\n# إنشاء Game ID جديد\ncurl -X POST \"https://kasrah-games.onrender.com/api/sdk/games/manage\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"gameId\": \"test-game-123\"}'\n```\n\n#### 2.2 اختبار Ads API\n```bash\n# الحصول على الإعلانات\ncurl -X GET \"https://kasrah-games.onrender.com/api/sdk/ads/manage?gameId=test-game-123\"\n\n# إنشاء إعلان جديد\ncurl -X POST \"https://kasrah-games.onrender.com/api/sdk/ads/manage\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"gameId\": \"test-game-123\",\n    \"adType\": \"video\",\n    \"title\": \"Test Ad\",\n    \"cpm\": 5.0\n  }'\n```\n\n#### 2.3 اختبار Analytics API\n```bash\n# الحصول على الإحصائيات\ncurl -X GET \"https://kasrah-games.onrender.com/api/sdk/analytics/stats?gameId=test-game-123&period=day\"\n```\n\n#### 2.4 اختبار Cloud Data API\n```bash\n# حفظ البيانات\ncurl -X POST \"https://kasrah-games.onrender.com/api/sdk/data/save-advanced\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"gameId\": \"test-game-123\",\n    \"userId\": \"user-123\",\n    \"key\": \"playerData\",\n    \"value\": {\"score\": 1000},\n    \"encrypt\": true\n  }'\n\n# تحميل البيانات\ncurl -X GET \"https://kasrah-games.onrender.com/api/sdk/data/save-advanced?gameId=test-game-123&userId=user-123&key=playerData\"\n```\n\n### 3. اختبار لوحة التحكم\n\n#### 3.1 اختبار صفحة الألعاب\n- [ ] الوصول إلى `/admin/sdk/games`\n- [ ] عرض قائمة الألعاب\n- [ ] إنشاء Game ID جديد\n- [ ] نسخ API Key\n- [ ] تحديث الإعدادات\n- [ ] عرض الإحصائيات\n\n#### 3.2 اختبار صفحة الإعلانات\n- [ ] الوصول إلى `/admin/sdk/ads`\n- [ ] اختيار لعبة\n- [ ] إنشاء إعلان جديد\n- [ ] تحديث الإعلان\n- [ ] حذف الإعلان\n- [ ] تفعيل/تعطيل الإعلان\n- [ ] عرض الإحصائيات\n\n#### 3.3 اختبار صفحة التحليلات\n- [ ] الوصول إلى `/admin/sdk/analytics`\n- [ ] اختيار لعبة\n- [ ] اختيار فترة زمنية\n- [ ] عرض الرسوم البيانية\n- [ ] عرض الإحصائيات\n- [ ] تحميل البيانات\n\n#### 3.4 اختبار صفحة البيانات السحابية\n- [ ] الوصول إلى `/admin/sdk/data`\n- [ ] اختيار لعبة\n- [ ] عرض قائمة البيانات\n- [ ] عرض تفاصيل البيانات\n- [ ] حذف البيانات\n- [ ] عرض الإحصائيات\n\n### 4. اختبار الأمان\n\n#### 4.1 اختبار المصادقة\n- [ ] التحقق من صلاحيات المسؤول\n- [ ] رفض الطلبات غير المصرح بها\n- [ ] التحقق من API Key\n\n#### 4.2 اختبار التشفير\n- [ ] تشفير البيانات عند الحفظ\n- [ ] فك التشفير عند التحميل\n- [ ] التحقق من سلامة البيانات\n\n#### 4.3 اختبار حدود الحجم\n- [ ] حفظ بيانات أقل من 2MB\n- [ ] رفض بيانات أكثر من 2MB\n- [ ] تتبع الحجم الإجمالي\n\n### 5. اختبار الأداء\n\n#### 5.1 اختبار السرعة\n- [ ] وقت تحميل SDK < 100ms\n- [ ] وقت عرض الإعلان < 500ms\n- [ ] وقت حفظ البيانات < 200ms\n- [ ] وقت تحميل البيانات < 200ms\n\n#### 5.2 اختبار الحمل\n- [ ] 100 طلب متزامن\n- [ ] 1000 طلب متتالي\n- [ ] 10000 سجل بيانات\n\n### 6. اختبار التوافقية\n\n#### 6.1 المتصفحات\n- [ ] Chrome\n- [ ] Firefox\n- [ ] Safari\n- [ ] Edge\n\n#### 6.2 الأجهزة\n- [ ] سطح المكتب\n- [ ] الجوال\n- [ ] اللوحات\n\n## نتائج الاختبار\n\n### نموذج تقرير الاختبار\n\n```\nتاريخ الاختبار: [التاريخ]\nالإصدار: 4.0.0\nالمختبر: [الاسم]\n\n| الاختبار | النتيجة | الملاحظات |\n|---------|--------|----------|\n| SDK Initialization | ✓ Pass | - |\n| Video Ads | ✓ Pass | - |\n| Rewarded Ads | ✓ Pass | - |\n| Cloud Data | ✓ Pass | - |\n| Analytics | ✓ Pass | - |\n| Admin Dashboard | ✓ Pass | - |\n| Security | ✓ Pass | - |\n| Performance | ✓ Pass | - |\n\nالملاحظات العامة:\n- النظام يعمل بشكل صحيح\n- جميع الميزات تعمل كما هو متوقع\n- الأداء مرضية\n```\n\n## قائمة التحقق قبل الإطلاق\n\n- [ ] جميع الاختبارات تمر بنجاح\n- [ ] التوثيق محدث\n- [ ] الأمان تم التحقق منه\n- [ ] الأداء مقبولة\n- [ ] المتصفحات المختلفة تم اختبارها\n- [ ] الأجهزة المختلفة تم اختبارها\n- [ ] المستخدمون تم إخطارهم\n- [ ] النسخة الاحتياطية تم إنشاؤها\n\n---\n\n**آخر تحديث:** فبراير 2026\n"
