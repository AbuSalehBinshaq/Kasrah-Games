# Kasrah Games - منصة ألعاب HTML5 و WebGL

منصة ألعاب ويب حديثة مبنية بـ Next.js 14 لتشغيل وإدارة ألعاب HTML5 و WebGL.

## المميزات

- 🎮 إدارة الألعاب (إضافة، تعديل، حذف)
- ⭐ نظام التقييمات والمراجعات
- 📂 تصنيف الألعاب حسب الفئات
- 👤 نظام المستخدمين والمصادقة
- 🔐 لوحة تحكم إدارية
- 🌐 دعم متعدد اللغات (عربي/إنجليزي)
- 📱 تصميم متجاوب (Responsive)
- 🔍 بحث متقدم
- ⚡ أداء عالي مع Next.js 14

## المتطلبات

- Node.js 18.0.0 أو أحدث
- PostgreSQL (أو أي قاعدة بيانات تدعمها Prisma)
- npm أو pnpm أو yarn

## التثبيت والتشغيل

### 1. تثبيت الـ Dependencies

```bash
npm install
# أو
pnpm install
# أو
yarn install
```

### 2. إعداد قاعدة البيانات

قم بنسخ ملف `.env.example` إلى `.env`:

```bash
cp .env.example .env
```

ثم قم بتعديل متغيرات البيئة في ملف `.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/kasrah_games?schema=public"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
```

### 3. إعداد Prisma

```bash
# توليد Prisma Client
npm run prisma:generate

# تشغيل Migrations
npm run prisma:migrate

# (اختياري) فتح Prisma Studio لإدارة قاعدة البيانات
npm run prisma:studio
```

### 4. تشغيل المشروع

```bash
# وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# تشغيل المشروع بعد البناء
npm run start
```

المشروع سيعمل على: `http://localhost:3000`

## هيكل المشروع

```
├── prisma/              # Prisma schema و migrations
├── public/              # الملفات الثابتة
│   ├── images/         # الصور
│   └── locales/        # ملفات الترجمة
├── src/
│   ├── app/            # Next.js App Router
│   │   ├── api/        # API Routes
│   │   ├── admin/      # لوحة التحكم الإدارية
│   │   ├── auth/       # صفحات المصادقة
│   │   └── games/      # صفحات الألعاب
│   ├── components/      # React Components
│   ├── hooks/          # Custom React Hooks
│   ├── lib/            # Utilities و Helpers
│   └── types/          # TypeScript Types
└── tests/              # الاختبارات
```

## المتغيرات البيئية

| المتغير | الوصف | مطلوب |
|---------|-------|-------|
| `DATABASE_URL` | رابط اتصال قاعدة البيانات | ✅ |
| `JWT_SECRET` | مفتاح التشفير لـ JWT | ✅ |
| `JWT_EXPIRES_IN` | مدة صلاحية الـ Token (افتراضي: 7d) | ❌ |
| `NEXT_PUBLIC_SITE_URL` | رابط الموقع | ❌ |
| `NEXT_PUBLIC_CONTACT_EMAIL` | البريد الإلكتروني للتواصل | ❌ |
| `RESEND_API_KEY` | مفتاح API لخدمة البريد (اختياري) | ❌ |

## الأوامر المتاحة

```bash
# التطوير
npm run dev              # تشغيل خادم التطوير

# البناء
npm run build            # بناء المشروع
npm run start            # تشغيل المشروع بعد البناء

# Prisma
npm run prisma:generate  # توليد Prisma Client
npm run prisma:migrate   # تشغيل Migrations
npm run prisma:studio    # فتح Prisma Studio

# الاختبارات
npm run test             # تشغيل الاختبارات
npm run test:watch       # تشغيل الاختبارات في وضع المراقبة
npm run test:coverage    # تقرير تغطية الاختبارات

# أخرى
npm run lint             # فحص الكود
```

## إنشاء حساب إداري

يمكنك استخدام ملف `promote_admin.js` لترقية مستخدم إلى إداري:

```bash
node promote_admin.js <user-email>
```

## التقنيات المستخدمة

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Database**: PostgreSQL مع Prisma ORM
- **Styling**: Tailwind CSS
- **Authentication**: JWT
- **Validation**: Zod
- **Forms**: React Hook Form
- **Icons**: Lucide React

## الأمان

- تشفير كلمات المرور باستخدام bcrypt
- JWT Tokens مع httpOnly cookies
- Security Headers (XSS Protection, CSRF, etc.)
- Input Validation مع Zod
- SQL Injection Protection (Prisma)

## النشر 🚀

**✅ نعم، يمكنك التعديل لاحقاً!** البناء لا يمنع التعديلات المستقبلية.

### نشر سريع:
- راجع [QUICK_DEPLOY.md](./QUICK_DEPLOY.md) للخطوات السريعة
- راجع [DEPLOYMENT.md](./DEPLOYMENT.md) للدليل الكامل

### الخيارات المتاحة:
1. **Vercel** (موصى به) - الأسهل والأسرع
2. **Railway** - قاعدة بيانات + استضافة في مكان واحد
3. **خادم خاص** - VPS/Cloud Server

### بعد النشر:
- كل تحديث في Git = نشر تلقائي (على Vercel)
- أو `git pull` + `npm run build` على الخادم

## الترخيص

هذا المشروع خاص.

## الدعم

للأسئلة والدعم، يرجى التواصل عبر: info@kasrahgames.example

---

**ملاحظة**: تأكد من تغيير جميع القيم الافتراضية في ملف `.env` قبل النشر في الإنتاج!

